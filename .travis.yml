language: python
matrix:
  fast_finish: true
  include:
    - python: 3.6
      env: TOX_ENV=unit-py36
    - python: 3.7
      env: TOX_ENV=unit-py37
    - python: 3.7
      env: TOX_ENV=lint

install:
  - pip install tox

script:
  - tox -e $TOX_ENV
